# For docker-compose.yml (standalone Docker replica of the THWS deployment).
# Copy to .env.compose and replace before `docker compose up` — dummy
# placeholders below, do not use in any reachable environment.
DB_PASSWORD=dummy-db-password-change-me
ADMIN_PASSWORD=dummy-admin-password-change-me
ZNUNY_APACHE_DOMAIN=ticket.thws.education
